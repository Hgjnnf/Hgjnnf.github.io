var options = {
    reset: true,
    delay: 100
}

ScrollReveal().reveal('#header-text', options);
ScrollReveal().reveal('projects', options);
ScrollReveal().reveal('.resume-reveal', options);
ScrollReveal().reveal('footer', options);